import RecreatePINModal from '.'

export default {
    title: 'RecreatePINModal',
    component: RecreatePINModal,
    args: {},
}

const Template = (args) => <RecreatePINModal {...args} />

export const PrimaryTemplate = Template.bind({})
