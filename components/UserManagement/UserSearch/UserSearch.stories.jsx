import UserSearch from '.'

export default {
    title: 'User Search',
    component: UserSearch,
    args: {},
}

const Template = (args) => <UserSearch></UserSearch>

export const PrimaryTemplate = Template.bind({})